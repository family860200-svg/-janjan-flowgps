# 260415 配抄演算法升級（待續）

**時間**：2026-04-15 清晨 06:41
**對話主題**：PM2 英吋工具演算法升級（3/4 孔庫存 + P2 備用庫存支援）

---

## ✅ 完成了什麼

- PM2 配抄工具四個版本全套升級（昨晚完成）：字體 16px、maxHole 鎖定、備料清單、黃金規格精確值
- PM1 英吋現況確認：GOLDEN 仍使用舊公式（302.5 乘數），尚未升級為精確值

---

## ❌ 還沒完成

### 🔴 主要：演算法升級（3/4 孔庫存）

**問題根因**：PM2 英吋序號 6（24 吋訂單）無法配抄，顯示 ⚠
- 原因：24 吋不是黃金規格，用主要庫存（P）1-2 孔湊不進 128.3-128.9 吋範圍
- 解法：加入第四優先（3 孔庫存）＋第五優先（4 孔庫存），同時加入 P2 備用庫存支援
- 驗證：24 + 25.67"(P2) + 39.37"(P) × 2 = 128.41 ✓

**需修改的檔案（8 個）**：
- `PM2配抄工具_英吋.html` — 主要目標（加 P2 + 3/4 孔庫存）
- `PM2配抄工具_公分.html` — 同步加 P2 + 3/4 孔庫存
- `PM2配抄工具_MM.html` — 只需加 3/4 孔庫存（P2 已有）
- `PM2配抄工具.html` — 只需加 3/4 孔庫存（台尺，無 P2）
- `PM1配抄工具_英吋.html` — 加 P2 + 3/4 孔庫存 + 修正 GOLDEN 精確值
- `PM1配抄工具_公分.html` — 加 P2 + 3/4 孔庫存
- `PM1配抄工具_MM.html` — 只需加 3/4 孔庫存（P2 已有）
- `PM1配抄工具.html` — 只需加 3/4 孔庫存（台尺，無 P2）

---

## 📍 下一步行動

到公司後繼續，直接從 **PM2配抄工具_英吋.html** 開始改：

### 具體變更（英吋 / 公分版）

**1. CSS 加入：**
```css
.type-P2 { color: #e74c3c; font-weight: bold; }
.tag-P2 { background: #fde8e8; color: #e74c3c; border-radius: 3px; padding: 1px 5px; font-size: 11px; }
```

**2. 圖例更新：**
```html
<span class="tag-W">W</span> 訂單　<span class="tag-P">P</span> 主要庫存　<span class="tag-P2">P2</span> 備用庫存
```

**3. JS GOLDEN 陣列（英吋）：**
```javascript
const GOLDEN_PRIMARY = [19.09,20.28,21.46,22.68,23.86,25.04,26.26,27.44,28.62,29.84,31.02,32.20,33.39,34.61,35.79,36.97,38.19,39.37,40.55,41.77,42.94];
const GOLDEN_SECONDARY = [19.69,20.87,22.09,23.27,25.67,26.85,28.03,29.21,30.43,31.61,32.80,34.02,35.20,36.38,37.60,41.18];
const GOLDEN_ALL = [...GOLDEN_PRIMARY,...GOLDEN_SECONDARY].sort((a,b)=>a-b);
```

**4. findInvSubset 加 pool 參數 + 新增 tryInv：**
```javascript
function findInvSubset(k,lo,hi,idx,chosen,cur,pool){...pool...}
function tryInv(k,lo,hi){
  let r=findInvSubset(k,lo,hi,0,[],0,GOLDEN_PRIMARY);
  if(r)return r.map(v=>({size:v,type:'P'}));
  r=findInvSubset(k,lo,hi,0,[],0,GOLDEN_ALL);
  if(r)return r.map(v=>({size:v,type:GOLDEN_PRIMARY.includes(v)?'P':'P2'}));
  return null;
}
```

**5. findRoll 加第四、第五優先：**
```javascript
// 第四優先：3孔庫存（5→4 孔共）
for(let n of[5,4]){let on=n-3;if(on<1||available.length<on)continue;let lo=Math.max(r(_min-3*_maxH),r(on*_minH)),hi=Math.min(r(_max-3*_minH),r(on*_maxH));if(lo>hi)continue;let oc=findOrderSubset(available,on,lo,hi,0,[],0);if(!oc)continue;let os=sum(oc);let ic=tryInv(3,r(_min-os),r(_max-os));if(ic)return[...oc.map(v=>({size:v,type:'W'})),...ic];}
// 第五優先：4孔庫存，1孔訂單（5孔共）
{let on=1;if(available.length>=on){let lo=Math.max(r(_min-4*_maxH),r(on*_minH)),hi=Math.min(r(_max-4*_minH),r(on*_maxH));if(lo<=hi){let oc=findOrderSubset(available,on,lo,hi,0,[],0);if(oc){let os=sum(oc);let ic=tryInv(4,r(_min-os),r(_max-os));if(ic)return[...oc.map(v=>({size:v,type:'W'})),...ic];}}}}
```

**6. render() P2 支援：**
- totalP2 變數
- note 更新（含備用庫存×N）
- summaryDiv 顯示 P2
- materialDiv plugCount 含 P2

---

## ✏️ 隨手一記

（本次對話無新增 manual_notes 記錄；昨日吃了晚餐後的胃藥）

---

## 💰 八大財富帳戶

- ⚒️ **技能**：系統性分析演算法邊界問題（24 吋無解的數學根因）
- 🫶 **服務**：為阿娟二號準備更完整的工具
- 💰 **金錢**：配抄工具持續精進，提升業務效率
